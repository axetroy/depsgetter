const http = require('http');
const net = require('net');

const { EventEmitter } = require('events');
require('dns');

var p = require('path'),
  c = require('process');

function main() {
  require('child_process');
  function os() {
    require('os');
  }
}
